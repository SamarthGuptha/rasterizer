A CPU based 3D rendering engine built entirely from scratch in Rust without external math libraries  
features custom linear algebra, view matrices, perspective projection, and backface culling.  

Dependencies: minifb  
